[project]
name = "hd6-bill-tracker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests", "jinja2", "pyyaml"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
